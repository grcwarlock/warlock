apiVersion: v1
kind: Service
metadata:
  name: {{ include "warlock.fullname" . }}-api
  labels:
    {{- include "warlock.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warlock.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
