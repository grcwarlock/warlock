apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "warlock.fullname" . }}-config
  labels:
    {{- include "warlock.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
